문제분석
    변수
     x,y
알고리즘
    변수선언 
        x,y에 정수 입력
    논리 선택(중복if)
     if x>y:
         if y!=0:
         "x//y"
         else:
         "y의 값이 0입니다"
      if x<y:
          "x+y"
      else:
          x*y
